// See previous starter; configure RZP_KEY_ID and RZP_KEY_SECRET in environment when deploying to Vercel or Netlify
const fetch = require('node-fetch');
// Implementations available in starter; copy them from provided earlier code when deploying.
module.exports = async (req, res) => { res.status(500).json({ error: 'Use provided API stubs from starter.' }); };
